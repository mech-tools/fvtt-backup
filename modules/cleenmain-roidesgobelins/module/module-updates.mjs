import { LOG_HEADER, LOG_HEADER_UPDATE_MODULE, MODULE_VERSION } from "./constants.mjs";

export async function updateAll(installedAdventureVersion){
    // if (foundry.utils.isNewerVersion("1.1.0", installedAdventureVersion)) await updateFrom100to110();
}

// Mise à jour de la version 1.0.0 vers la version 1.1.0
export async function updateFrom100to110() {
}